import React from 'react';
import styled from 'styled-components';

const Error = () => {
  return <ErrorContainer>Looks like something when wrong</ErrorContainer>;
};

const ErrorContainer = styled.div``;

export default Error;

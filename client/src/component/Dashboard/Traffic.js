import React from 'react';
import styled from 'styled-components';

const Traffic = () => {
  return <TrafficContainer>Traffic</TrafficContainer>;
};

const TrafficContainer = styled.div``;

export default Traffic;
